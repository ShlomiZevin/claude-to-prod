# Copy to server/.env and fill in. NEVER commit the real .env (public repo).
PORT=8080

# AI providers (see slides s19/s20 - Claude orchestrates other models)
OPENAI_API_KEY=
LEONARDO_API_KEY=
ANTHROPIC_API_KEY=

# Google Cloud / Firebase
GCLOUD_PROJECT=claude-to-prod
